{% extends 'Adilet/home.html.twig' %}

{% block title %}Pupil{% endblock %}

{% block body %}
    <h1>Pupil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pupil.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pupil.name }}</td>
            </tr>
            <tr>
                <th>Surename</th>
                <td>{{ pupil.surename }}</td>
            </tr>
            <tr>
                <th>Class</th>
                <td>{{ pupil.class }}</td>
            </tr>
            <tr>
                <th>School</th>
                <td>{{ pupil.school }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pupil_index') }}">back to list</a>

    <a href="{{ path('pupil_edit', {'id': pupil.id}) }}">edit</a>

    {{ include('Adilet/_delete_form.html.twig') }}
{% endblock %}
